<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:input=”clr-namespace:Plugin.InputKit.Shared.Controls;assembly=Plugin.InputKit”
             x:Class="test_app_take_2.ChooseFiltersPage">

    <StackLayout>
        <Frame BackgroundColor="#ccb3ff" Padding="24" CornerRadius="0">
            <Label Text="Choose Filters" 
                   HorizontalTextAlignment="Center" 
                   TextColor="Black" 
                   FontSize="32"/>
        </Frame>

        <Button x:Name="VeganOptions"
                Text="Vegan Options"
                Clicked="VeganOptions_Clicked"/>
        <Button x:Name="BlackOwned"
                Text="Black Owned"
                Clicked="BlackOwned_Clicked"/>

        <Grid>
            <Button Grid.Column="1"
                    Text="Continue"
                    BackgroundColor="#ccb3ff"
                    FontSize="25"
                    HeightRequest="110" />
        </Grid>

    </StackLayout>

</ContentPage>
